import React from "react";

const Marks = () => {
  const [marks, setMarks] = useState([45, 56, 85, 95, 48, 31, 34, 50]);
  return <div>
    {marks.map(stud => {
        return
    })}
  </div>;
};

export default Marks;
